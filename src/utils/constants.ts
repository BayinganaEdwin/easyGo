const PERSIST_KEY = 'EasyGo';
const DATE_FORMAT = 'DD MMM YYYY';
const TOKEN_NAME = 'easyGo_token';

const TIME_FORMAT = 'HH:mm A';

export { DATE_FORMAT, TIME_FORMAT, PERSIST_KEY, TOKEN_NAME };
